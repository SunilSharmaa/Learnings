/* Basic #1 - Enter roll and Percentage*/
#include <stdio.h>

int main()
{
    int roll;
    float per;
    printf(" Enter Roll no & Percentage= ");
    scanf("%d %f", &roll, &per);
    
    printf(" Roll no is = %d\n Percentage is = %f",roll,per);

    return 0;
}
