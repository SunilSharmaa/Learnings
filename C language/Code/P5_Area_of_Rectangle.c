/* Basic #5 - Area of Rectangle*/
#include <stdio.h>

int main()
{
   
    int length, breadth;
    printf("Length and Breadth of a Rectangle\n");
    scanf("%d%d", &length, &breadth);
    
    printf("Area of Recangle is = %d",length*breadth);

    return 0;
}
